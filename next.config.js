// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
 images: {
  domains: ['images.pexels.com'], // Add the domain of your image source here
 }
}

module.exports = nextConfig;
